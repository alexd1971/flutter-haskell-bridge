name: flutter_haskell_plugin
description: Flutter FFI package backed by a cross-compiled Haskell library.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.12.0 <4.0.0"

dependencies:
  code_assets: ^1.0.0
  hooks: ^1.0.0
